docker exec -ti magento-docker-magento2 /bin/bash -c \
	"cd /bitnami/magento && \
	composer update && \
	chown daemon:root vendor -R && \
	bin/magento setup:upgrade && \
	bin/magento setup:di:compile && \
	bin/magento cache:flush && \
	chown daemon:root . -R"
